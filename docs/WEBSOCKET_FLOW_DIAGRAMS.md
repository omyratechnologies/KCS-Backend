# 📊 WebSocket Integration Flow Diagrams

**Visual guide to understanding WebSocket flows**

---

## 🔐 Authentication Flow

```
┌─────────────┐
│   User      │
│  Opens App  │
└──────┬──────┘
       │
       │ 1. Login via REST API
       ▼
┌─────────────────┐
│  POST /auth     │
│  (username/pwd) │
└──────┬──────────┘
       │
       │ 2. Receive JWT Token
       ▼
┌─────────────────┐
│ Store Token     │
│ localStorage    │
└──────┬──────────┘
       │
       │ 3. Connect WebSocket
       ▼
┌─────────────────────────┐
│ io('ws://server:4501',  │
│   auth: { token })      │
└──────┬──────────────────┘
       │
       │ 4. Server validates token
       ▼
   ┌───────┐
   │Success│
   └───┬───┘
       │
       │ 5. Connection established
       ▼
┌──────────────────┐
│ emit('connect')  │
│ App is ready!    │
└──────────────────┘
```

---

## 💬 Chat Message Flow

### Sending a Message

```
┌──────────────┐
│ User types   │
│ "Hello!"     │
└──────┬───────┘
       │
       │ 1. User clicks Send
       ▼
┌───────────────────────────────┐
│ POST /api/chat/rooms/123/msgs │
│ Body: { content: "Hello!" }   │
└──────┬────────────────────────┘
       │
       │ 2. REST API processes
       ▼
┌──────────────────┐
│ Save to Database │
└──────┬───────────┘
       │
       │ 3. ChatService broadcasts
       ▼
┌─────────────────────────────────┐
│ SocketService.broadcastMessage │
│   (roomId, message)             │
└──────┬──────────────────────────┘
       │
       │ 4. WebSocket emits to all room members
       ▼
  ┌────────────────────────────────┐
  │  socket.to('room_123')         │
  │    .emit('new-chat-message')   │
  └────┬───────────────────────────┘
       │
       ├──────────┬──────────┬──────────┐
       ▼          ▼          ▼          ▼
   Client A   Client B   Client C   Sender
   receives   receives   receives   receives
```

### Receiving a Message

```
┌──────────────────────┐
│ WebSocket receives   │
│ 'new-chat-message'   │
└──────┬───────────────┘
       │
       │ 1. Event listener triggered
       ▼
┌───────────────────────────────┐
│ socket.on('new-chat-message', │
│   (data) => { ... })          │
└──────┬────────────────────────┘
       │
       │ 2. Extract message data
       ▼
┌──────────────────┐
│ const message =  │
│   data.data      │
└──────┬───────────┘
       │
       │ 3. Update UI
       ▼
┌──────────────────┐
│ Add to chat list │
│ Scroll to bottom │
│ Play sound       │
└──────┬───────────┘
       │
       │ 4. If window not focused
       ▼
┌──────────────────┐
│ Show desktop     │
│ notification     │
└──────────────────┘
```

---

## 🎥 Meeting Join Flow

```
┌──────────────┐
│ User clicks  │
│ "Join Meet"  │
└──────┬───────┘
       │
       │ 1. Emit join-meeting
       ▼
┌────────────────────────────┐
│ socket.emit('join-meeting',│
│   { meetingId: '123' })    │
└──────┬─────────────────────┘
       │
       │ 2. Server validates
       ▼
   ┌─────────────────┐
   │ • Meeting exists│
   │ • User has access│
   │ • Not full      │
   │ • Password OK   │
   └───┬─────────────┘
       │
       │ 3. Success
       ▼
┌────────────────────────────┐
│ Server actions:            │
│ • Join socket room         │
│ • Create participant record│
│ • Setup WebRTC router      │
└──────┬─────────────────────┘
       │
       │ 4. Emit to new participant
       ▼
┌──────────────────────────────┐
│ socket.emit('meeting-joined',│
│   {                          │
│     meeting: {...},          │
│     participantId: '...',    │
│     participants: [...],     │
│     webrtcConfig: {...}      │
│   })                         │
└──────┬───────────────────────┘
       │
       │ 5. Notify others
       ▼
┌───────────────────────────────┐
│ socket.to(meetingId)          │
│   .emit('participant-joined') │
└──────┬────────────────────────┘
       │
       │ 6. Client receives
       ▼
┌──────────────────────┐
│ Initialize UI:       │
│ • Show participants  │
│ • Setup video grid   │
│ • Enable controls    │
│ • Start WebRTC       │
└──────────────────────┘
```

---

## ⌨️ Typing Indicator Flow

```
┌──────────────────┐
│ User types in    │
│ message box      │
└────┬─────────────┘
     │
     │ onChange event
     ▼
┌─────────────────────────┐
│ Debounced function      │
│ (wait 300ms)            │
└────┬────────────────────┘
     │
     │ 1. Emit typing=true
     ▼
┌──────────────────────────┐
│ socket.emit('chat-typing'│
│   { roomId, isTyping: T }│
└────┬─────────────────────┘
     │
     │ 2. Server broadcasts
     ▼
┌────────────────────────────┐
│ socket.to(room)            │
│   .emit('chat-user-typing')│
└────┬───────────────────────┘
     │
     ├─────────┬─────────┐
     ▼         ▼         ▼
   User A   User B   User C
   sees     sees     sees
   "You're typing..."
     │
     │ 3. After 3 seconds
     ▼
┌──────────────────────────┐
│ Auto-emit typing=false   │
└────┬─────────────────────┘
     │
     │ 4. Server broadcasts
     ▼
┌────────────────────────────┐
│ socket.to(room)            │
│   .emit('chat-user-typing')│
│   (isTyping: false)        │
└────┬───────────────────────┘
     │
     ▼
All users: indicator hidden
```

---

## ✅ Read Receipts Flow

```
┌──────────────────┐
│ User scrolls to  │
│ bottom of chat   │
└────┬─────────────┘
     │
     │ 1. Detect unread messages
     ▼
┌───────────────────────┐
│ Collect message IDs   │
│ ['msg1', 'msg2']      │
└────┬──────────────────┘
     │
     │ 2. Emit seen event
     ▼
┌──────────────────────────────┐
│ socket.emit('mark-msgs-seen',│
│   { roomId, messageIds })    │
└────┬─────────────────────────┘
     │
     │ 3. Server updates database
     ▼
┌────────────────────────┐
│ Mark messages as seen  │
│ by this user           │
└────┬───────────────────┘
     │
     │ 4. Broadcast to room
     ▼
┌──────────────────────────┐
│ socket.to(room)          │
│   .emit('messages-seen') │
└────┬─────────────────────┘
     │
     ├──────────┬──────────┐
     ▼          ▼          ▼
  Sender   Other 1   Other 2
  updates  updates   updates
  ✓✓ icon  UI        UI
```

---

## 🔄 Reconnection Flow

```
┌─────────────────┐
│ Network drops   │
└────┬────────────┘
     │
     │ 1. disconnect event
     ▼
┌──────────────────────┐
│ socket.on('disconnect│
│   Show: "Offline"    │
└────┬─────────────────┘
     │
     │ 2. Auto reconnect attempts
     ▼
┌──────────────────────────┐
│ Every 1 second (max 5x)  │
└────┬─────────────────────┘
     │
     │ 3. reconnect_attempt
     ▼
┌──────────────────────────┐
│ Show: "Reconnecting..."  │
└────┬─────────────────────┘
     │
     │ 4. Success
     ▼
┌────────────────────┐
│ socket.on('reconnect'│
│ Hide offline msg   │
└────┬───────────────┘
     │
     │ 5. Re-join rooms
     ▼
┌─────────────────────────┐
│ socket.emit('join-rooms'│
│   { roomIds: [...] })   │
└────┬────────────────────┘
     │
     │ 6. Sync state
     ▼
┌───────────────────────┐
│ • Fetch missed messages│
│ • Update online users │
│ • Resume functionality │
└───────────────────────┘
```

---

## 🎬 Media Toggle Flow (Meeting)

```
┌──────────────────┐
│ User clicks      │
│ Camera button    │
└────┬─────────────┘
     │
     │ 1. Toggle local state
     ▼
┌─────────────────────┐
│ setCameraOn(!on)    │
└────┬────────────────┘
     │
     │ 2. Update local stream
     ▼
┌──────────────────────┐
│ localVideo.enabled = │
│   cameraOn           │
└────┬─────────────────┘
     │
     │ 3. Notify server
     ▼
┌──────────────────────────────┐
│ socket.emit('media-status-   │
│   update', {                 │
│     meetingId,               │
│     video: cameraOn,         │
│     audio: micOn,            │
│     screenSharing: false     │
│   })                         │
└────┬─────────────────────────┘
     │
     │ 4. Server broadcasts
     ▼
┌─────────────────────────────────┐
│ socket.to(meeting)              │
│   .emit('participant-media-     │
│     updated', { video: T/F })   │
└────┬────────────────────────────┘
     │
     ├──────────┬──────────┬──────────┐
     ▼          ▼          ▼          ▼
  Viewer 1  Viewer 2  Viewer 3  Viewer 4
  updates   updates   updates   updates
  video     icon      UI        tile
```

---

## 🚪 Room Join/Leave Flow

### Joining Rooms

```
┌─────────────┐
│ App Starts  │
└──────┬──────┘
       │
       │ 1. Fetch user's rooms (REST API)
       ▼
┌──────────────────────┐
│ GET /api/chat/rooms  │
└──────┬───────────────┘
       │
       │ 2. Response: [room1, room2, ...]
       ▼
┌───────────────────────┐
│ Extract room IDs      │
│ ['room1', 'room2']    │
└──────┬────────────────┘
       │
       │ 3. Join via WebSocket
       ▼
┌────────────────────────────┐
│ socket.emit('join-chat-    │
│   rooms', { roomIds })     │
└──────┬─────────────────────┘
       │
       │ 4. Server adds to rooms
       ▼
┌──────────────────────────┐
│ socket.join('room_1')    │
│ socket.join('room_2')    │
└──────┬───────────────────┘
       │
       │ 5. Confirmation
       ▼
┌────────────────────────────┐
│ socket.emit('chat-rooms-   │
│   joined', { success: T }) │
└──────┬─────────────────────┘
       │
       │ 6. Now receiving messages
       ▼
┌────────────────┐
│ Ready to chat! │
└────────────────┘
```

### Leaving a Room

```
┌─────────────────┐
│ User navigates  │
│ away from chat  │
└──────┬──────────┘
       │
       │ 1. Component unmount
       ▼
┌────────────────────────┐
│ useEffect cleanup      │
│ return () => { ... }   │
└──────┬─────────────────┘
       │
       │ 2. Emit leave event
       ▼
┌─────────────────────────┐
│ socket.emit('leave-     │
│   chat-room', { roomId })│
└──────┬──────────────────┘
       │
       │ 3. Server removes from room
       ▼
┌──────────────────────┐
│ socket.leave('room') │
└──────┬───────────────┘
       │
       │ 4. No longer receives messages
       ▼
┌────────────────┐
│ Clean exit!    │
└────────────────┘
```

---

## 📱 Online Status Flow

```
┌──────────────┐
│ User opens   │
│ app          │
└──────┬───────┘
       │
       │ 1. WebSocket connects
       ▼
┌─────────────────────┐
│ Auto-set to online  │
└──────┬──────────────┘
       │
       │ 2. Broadcast to contacts
       ▼
┌────────────────────────────┐
│ socket.broadcast.emit(     │
│   'chat-user-status-update'│
│   { userId, status: online }│
│ )                          │
└──────┬─────────────────────┘
       │
       │ 3. User changes status
       ▼
┌─────────────────────────┐
│ User selects "Away"     │
└──────┬──────────────────┘
       │
       │ 4. Emit status change
       ▼
┌──────────────────────────┐
│ socket.emit('update-     │
│   chat-status',          │
│   { status: 'away' })    │
└──────┬───────────────────┘
       │
       │ 5. Server broadcasts
       ▼
┌────────────────────────────┐
│ All contacts see           │
│ user status: Away 🟡       │
└──────┬─────────────────────┘
       │
       │ 6. User disconnects
       ▼
┌────────────────────────────┐
│ Auto-broadcast             │
│ status: offline            │
│ lastSeen: timestamp        │
└────────────────────────────┘
```

---

## 🎯 Summary Diagrams

### Complete Chat Message Lifecycle

```
┌──────────┐
│   USER   │
└────┬─────┘
     │
     │ Types & Sends
     ▼
┌──────────────┐
│  REST API    │ ────────────┐
│  (Saves msg) │             │
└────┬─────────┘             │
     │                       │
     │ Triggers              │ Database
     ▼                       │
┌──────────────┐             │
│ ChatService  │ ◄───────────┘
└────┬─────────┘
     │
     │ Broadcasts
     ▼
┌──────────────┐
│SocketService│
└────┬─────────┘
     │
     │ WebSocket
     ├──────────┬──────────┬──────────┐
     ▼          ▼          ▼          ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│Client 1 │ │Client 2 │ │Client 3 │ │ Sender  │
│Receives │ │Receives │ │Receives │ │Receives │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
```

### Meeting + WebRTC Flow

```
┌──────────┐
│  Join    │
│  Meeting │
└────┬─────┘
     │
     ├─────────────────────┬────────────────────┐
     │                     │                    │
     │ Socket.IO           │ WebRTC             │ REST API
     │ (Signaling)         │ (Media)            │ (Data)
     │                     │                    │
     ▼                     ▼                    ▼
┌──────────┐         ┌──────────┐        ┌──────────┐
│ join-    │         │ Audio/   │        │ Participant│
│ meeting  │         │ Video    │        │ Record    │
└────┬─────┘         │ Stream   │        └──────────┘
     │               └──────────┘
     │
     ▼
┌──────────┐
│ Chat,    │
│ Reactions│
│ Controls │
└──────────┘
```

---

## 🎓 Key Takeaways

### Event Flow Principles

1. **Authentication**: Always connect with JWT token
2. **REST for Write**: Send messages via REST API
3. **WebSocket for Read**: Receive updates via WebSocket
4. **Broadcast Pattern**: Server broadcasts to room members
5. **State Sync**: Always rejoin rooms after reconnect

### Performance Tips

1. **Debounce typing indicators** (300ms)
2. **Batch read receipts** (don't mark each individually)
3. **Leave rooms** when not needed
4. **Handle reconnection** gracefully
5. **Use room patterns** efficiently

---

**Document Version**: 1.0  
**Last Updated**: October 25, 2025  
**Maintained By**: Backend Team
